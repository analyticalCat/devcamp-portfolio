<h1>Portfolio Items</h1>

<a href="portfolios/new">Create New Item</a>

<%= link_to "Create New Item", new_portfolio_path %>

<% @portfolio_items.each do |port| %>
  <p><%= link_to port.title, portfolio_show_path(port) %></p>
  <p><%= port.subtitle %></p>
  <p><%= port.body %></p>
  <p><%= image_tag port.thumb_image unless port.thumb_image.nil?%></p>
  <%= link_to "Edit Portfolio Item", edit_portfolio_path(port) %>
  <%= link_to 'Delete portfolio item', portfolio_path(port), method: :delete, data: {confirm: 'Are you sure?'} %>
<% end %>